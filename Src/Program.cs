using System.CodeDom;
using TECHCOOL.UI;

namespace App
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            Screen.Display(new MenuScreen());
            // var person = new Person("I am number One", "F*uck off", "22 22 22 22", "dk@dk.dk", new Address("Denmark", "9000", "Pool", "dabsj", "22"), Role.Customer, "");
            // var db = new Database();
            // db.InsertPerson(person);
            // db.GetPerson();
        }
    }
}

